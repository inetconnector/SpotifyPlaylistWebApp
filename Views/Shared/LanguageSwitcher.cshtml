@using Microsoft.AspNetCore.Localization
@inject IHttpContextAccessor HttpContextAccessor
@{
    var rqf = HttpContextAccessor.HttpContext?.Features?.Get<IRequestCultureFeature>();
    var current = rqf?.RequestCulture?.UICulture?.Name ?? "en-US";
    var returnUrl = HttpContextAccessor.HttpContext?.Request?.Path + HttpContextAccessor.HttpContext?.Request?.QueryString;
}

<div class="lc-switch" role="group">
    <a href="/Language/SetLanguage?culture=de-DE&returnUrl=@returnUrl"
       class="lc-btn @(current == "de-DE" ? "active" : "")"
       aria-current="@(current == "de-DE" ? "true" : "false")"
       title="Deutsch">
        <span class="flag">🇩🇪</span>
        <span class="label">DE</span>
    </a>

    <a href="/Language/SetLanguage?culture=en-US&returnUrl=@returnUrl"
       class="lc-btn @(current == "en-US" ? "active" : "")"
       aria-current="@(current == "en-US" ? "true" : "false")"
       title="English">
        <span class="flag">🇺🇸</span>
        <span class="label">EN</span>
    </a>

    <a href="/Language/SetLanguage?culture=es-ES&returnUrl=@returnUrl"
       class="lc-btn @(current == "es-ES" ? "active" : "")"
       aria-current="@(current == "es-ES" ? "true" : "false")"
       title="Español">
        <span class="flag">🇪🇸</span>
        <span class="label">ES</span>
    </a>
</div>

<style>
.lc-switch {
    display: flex;
    gap: .5rem;
    align-items: center;
    justify-content: flex-end;
    flex-wrap: nowrap;
}

.lc-btn {
    display: inline-flex;
    align-items: center;
    gap: .35rem;
    padding: .4rem .75rem;
    border-radius: .65rem;
    border: 1px solid rgba(255,255,255,.35);
    color: #fff;
    text-decoration: none;
    background: rgba(255,255,255,.12);
    backdrop-filter: saturate(140%);
    font-weight: 500;
    transition: transform .08s ease, background .2s ease, border-color .2s ease, box-shadow .2s ease;
}

.lc-btn:hover {
    background: rgba(255,255,255,.24);
    border-color: rgba(255,255,255,.55);
    box-shadow: 0 4px 16px rgba(15,32,39,.35);
}

.lc-btn.active {
    border-color: rgba(255,255,255,.85);
    background: rgba(255,255,255,.32);
    font-weight: 700;
}

.flag {
    font-size: 1rem;
    line-height: 1;
}

.label {
    font-size: .9rem;
}
</style>